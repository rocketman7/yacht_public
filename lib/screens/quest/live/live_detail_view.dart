import 'package:flutter/material.dart';

class LiveDetailView extends StatelessWidget {
  const LiveDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
