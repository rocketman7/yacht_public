import 'package:flutter/material.dart';

class NewLiveDetailView extends StatelessWidget {
  const NewLiveDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
